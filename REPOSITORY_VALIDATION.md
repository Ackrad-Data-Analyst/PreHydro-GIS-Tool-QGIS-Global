# Repository validation

Validated on 2026-09-06: **10 tests passed**. The standalone release builder was also configured to write repository-local artifacts under `dist/`.

Pure helper logic is testable in standard Python. Live Processing execution requires QGIS 3.44 or newer with Native, GDAL, and GRASS providers, plus network access for automatic public-data acquisition.
